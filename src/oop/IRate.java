package oop;

public interface IRate {
	// Interface are similar to inheritance. Difference is inheritance has parent-child relationship whereas interface has contract
	// Write the class outline
	public void setRate();
	public void increaseRate();
}
